# TGAC environment example

# === Core application settings ===
ADMIN_TG_USERNAME=admin_username
TELEGRAM_BOT_TOKEN=0000000000:example-token
TELEGRAM_DEEPLINK_TTL_MIN=10
OPENAI_API_KEY=sk-example
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=240
SMS_ACTIVATE_API_KEY=sa-example
SMS_ACTIVATE_POLL_INTERVAL_SECONDS=30
SMS_ACTIVATE_MAX_POLL_ATTEMPTS=10
BRIGHTDATA_USERNAME=brightdata-user
BRIGHTDATA_PASSWORD=brightdata-pass
TZ=Europe/Amsterdam
BASE_URL=https://tg.vpn-gpt.store

# === Database and crypto ===
DB_URL=sqlite:////workspace/sanych/tgac/data/db.sqlite3
SESSION_SECRET_KEY=change-me-to-fernet-key  # например: python -c "import secrets, base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"

# === Limits and throttling ===
LOG_RETENTION_DAYS=7
MAX_CHANNELS_PER_ACCOUNT=50
COMMENT_COLLISION_LIMIT_PER_POST=1
MAX_ACTIVE_THREADS_PER_ACCOUNT=50
COMMENT_VISIBILITY_STALE_MINUTES=5
ACCOUNT_HEALTHCHECK_INTERVAL_MINUTES=180
ACCOUNT_HEALTHCHECK_BATCH_SIZE=100
CHANNEL_SCAN_INTERVAL_MINUTES=15
CHANNEL_SCAN_BATCH_SIZE=50
DEFAULT_PROJECT_QUOTA=5

# === Worker runtime ===
WORKER_SHARDS=1
WORKER_SHARD=0
MAX_CONCURRENCY=10
LOG_LEVEL=INFO
EVENTS_LOG_PATH=tgac/logs/events.jsonl

# === Optional integrations ===
# TELEGRAM webhook configuration can be added here if needed
# Additional custom settings may be appended below
